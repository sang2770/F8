const init={
    cars:["BMW"]
}
export default function reducer(state = init, action, args)
{
    switch(action)
    {
        case "Create":
            break;
        default:
            return state;
    }
}